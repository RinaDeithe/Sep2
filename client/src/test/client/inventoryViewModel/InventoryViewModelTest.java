package test.client.inventoryViewModel;

import junit.framework.TestCase;
import org.junit.jupiter.api.Test;

public class InventoryViewModelTest extends TestCase
{
  @Test void canSleep() throws InterruptedException
  {
    /*
    InventoryViewModel vm = new InventoryViewModel(new ModelManager(new RMIClient()));
    assertTrue(vm.showProducts().isEmpty()); //Vi tjekker om metoden er tom (som den skal være), og at der ikke sker error når den skal sleep
    */
  }
}
